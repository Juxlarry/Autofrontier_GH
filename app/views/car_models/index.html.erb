<style>
  .blocktext {
      margin-left: auto;
      margin-right: auto;
  
      margin: 0;
    position: relative;
    left: 50%;
    margin-right: -50%;
  }
</style>
<main>
  <section id="main-content">
    <section class="wrapper">
      <div class="row">
        <div class="col-12">
          <h1 class="blocktext">Car Model </h1>
          <div class="separator mb-5"></div>
        </div>
      </div>
      <div class="row mb-4">
        <div class="col-12 mb-4">
          <div class="card">
            <div class="card-body">
              <div class="table-responsive bs-example widget-shadow">
                <table class="table table-bordered">
                  <thead>
                    <tr>
                      <th>Model Name</th>
                      <th>Make ID</th>
                      <th>Comments</th>
                      <th colspan="3"></th>
                    </tr>
                  </thead>
                  <tbody>
                    <% @car_models.each do |car_model| %>
                      <tr>
                        <td><%= car_model.car_model_name %></td>
                        <td><%= car_model.make_name %></td>
                        <td><%= car_model.model_comments %></td>
                       
                        <td><span id="badgeSpan" class="badge badge-primary"><%= link_to 'Show', car_model, id: "badgeSpan" %></span></td>
                        <td><span id="badgeSpan" class="badge badge-warning"><%= link_to 'Edit', edit_car_model_path(car_model) %></span></td>
                        <td><span id="badgeSpan" class="badge badge-danger"><%= link_to 'Delete', car_model, method: :delete, data: { confirm: 'Are you sure?' } %></span></td>
                      </tr>
                    <% end %>
                  </tbody>
                </table>
                <br>
              </div>
            </div>
          </div>
          <div class="blocktext" style="margin-top:5%; color:white">
            <%= link_to 'Add Car Model', new_car_model_path , class:'btn btn-primary', style:'color:white' %>
          </div>
        </div>
      </div>
    </section>
  </section>
</main>