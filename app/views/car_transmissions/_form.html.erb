<%= form_with(model: car_transmission) do |form| %>
  <% if car_transmission.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(car_transmission.errors.count, "error") %> prohibited this car_transmission from being saved:</h2>

      <ul>
        <% car_transmission.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :status %>
    <%= form.check_box :status %>
  </div>

  <div class="field">
    <%= form.label :comments %>
    <%= form.text_area :comments %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
